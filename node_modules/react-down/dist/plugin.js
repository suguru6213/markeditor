'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = combinePlugins;

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function makeContinuation(createElement) {
  return function (next) {
    return function (type, props, children, token) {
      var element = createElement(type, props, children, token);
      return element ? element : next(type, props, children, token);
    };
  };
}

function reactCreateElement(type, props, children) {
  return _react2.default.createElement(type, props, children);
}

function combinePlugins() {
  for (var _len = arguments.length, plugins = Array(_len), _key = 0; _key < _len; _key++) {
    plugins[_key] = arguments[_key];
  }

  var createElement = plugins.map(makeContinuation).reduce(function (createElement, continuation) {
    return continuation(createElement);
  }, reactCreateElement);

  return function (transform) {
    return function (src) {
      return transform(src, createElement);
    };
  };
}